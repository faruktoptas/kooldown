name: Android CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Run UI Tests
      uses: faruktoptas/android-github-actions/emulator@v1.0.0
