name: Android CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build with Gradle
      uses: vgaidarji/android-github-actions-build@v1.0.1
      with:
        args: "./gradlew connectedAndroidTest"
